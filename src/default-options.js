'use strict';

const defaultOptions = {
    encoding: 'utf8',
    cursor: true,
    x: 0,
    y: 0,
    bgColor: 'black',
    fgColor: 'white',
    styles: new Set(),
    wrap: true,
    scroll: true,
    lock: true
};

module.exports = defaultOptions;
